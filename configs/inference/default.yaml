# configs/inference.yaml
model_ckpt: "outputs/best-change-model.ckpt"
input_h5: "data/generated/change_dataset_batch_0009.h5"
output_h5: "results.h5"
device: "cuda"

# Keep the model definition here to allow Hydra to understand the architecture if needed
model:
  _target_: src.models.system.ChangeDetectionSystem
  in_dim: 2
  hidden_dim: 128